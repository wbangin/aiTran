# 项目协作约定

## 版本号

- 用户要求每完成一轮项目修改，都递增一次版本号，让用户能确认 Chrome 加载的是最新版本。默认递增补丁版本，例如 `0.3.9` → `0.3.10`；同一轮中的测试和修正不重复递增。仅分析或答疑、未修改项目时无需升级。
- 同步更新 `package.json`、`package-lock.json` 的根版本与根包版本、`wxt.config.ts` 中的 manifest 版本、弹窗和隐私页的版本展示，以及 README 的当前版本标题。
- 在 `CHANGELOG.md` 中记录对应版本、日期和本轮变更。
- 交付前重新构建 Chrome 和 Firefox 安装目录，并核对生成的 manifest 版本及界面版本展示。最终回复说明新版本号，提醒用户在 Chrome 扩展管理页重新加载插件。
