# aiTran AI 翻译提示词设计

本文记录对原发行包 `immersive-translate/dist/chrome/default_config.json` 与打包脚本的分析，并说明 aiTran 的兼容实现。

## 原发行包的提示词层次

原发行包的通用 AI 服务配置包含四类主要提示词：

1. **系统提示词 `systemPrompt`**
   - 定义模型是目标语言母语翻译者。
   - 要求只输出译文，不解释。
   - 要求维持段落数量和格式。
   - 要求保留 HTML 标签、代码、专有名词和占位符。
   - 兼容模式使用 `%%` 作为多段分隔符。
   - 可插入标题、摘要、术语与风格变量。

2. **单段提示词 `prompt`**
   - 用于单个普通网页段落、划词或输入框文本。
   - 默认形式是要求翻译为 `{{to}}` 并只输出译文，然后附加 `{{text}}`。

3. **字幕提示词 `subtitlePrompt`**
   - 用于字幕场景。
   - 默认形式较短，强调翻译为 `{{to}}`，正文为 `{{text}}`。

4. **多段提示词 `multiplePrompt`**
   - 用于一次请求包含多个段落的兼容批处理。
   - 文本之间使用独立一行 `%%` 分隔。
   - 返回结果也必须保持相同数量与顺序。

原配置中常见的兼容模式上限是每次 4 段，字幕也是 4 段。Temperature 通常为 0。

## 新式批量协议

原发行包还包含一套较新的 `aiBatch` 协议：

- `taskSystemPrompt` 定义翻译任务。
- `protocolSystemPrompt` 定义多条目的 ID 协议。
- 每个源条目带唯一 ID。
- 输入以 `[[source_end]]` 结束。
- 输出必须返回每个 ID 一次。
- 可以额外提取术语，供后续批次保持一致。
- 默认每批 8 项，字幕每批 8 项，上限 32 项。
- 如果输出无法恢复，可按预算降级到备用服务。

aiTran 当前没有复制其术语提取与付费备用服务逻辑，而是实现了更透明的两种模式：

- **兼容模式**：使用 `%%` 分隔，多段一次请求；段数不匹配时自动逐段恢复。
- **逐段模式**：每段独立请求，稳定性更高，但请求数量更多。

## aiTran 支持的变量

- `{{to}}`：目标语言名称
- `{{from}}`：源语言名称
- `{{text}}`：待翻译文本
- `{{title}}`：网页标题或文档文件名
- `{{title_prompt}}`：存在标题时生成标题上下文
- `{{summary_prompt}}`：摘要上下文；未提供时为空
- `{{terms_prompt}}`：术语上下文；未提供时为空
- `{{imt_style_guide}}`：风格指导；未提供时为空

## 场景选择

- 网页只有一个段落：单段提示词
- 网页一次发送多个段落：多段提示词
- 划词、鼠标悬停、输入框：单段提示词
- SRT/VTT 字幕：Subtitle Prompt
- 文本和普通文档：根据批次段落数选择单段或多段提示词

## 安全与缓存

- API Key 只保存在浏览器本地。
- 更改模型、Temperature、批量模式或任意提示词后，缓存命名空间会改变，不会继续读取旧提示词产生的缓存。
- 多段返回数量错误时，aiTran 会自动按单段提示词逐项重试。
